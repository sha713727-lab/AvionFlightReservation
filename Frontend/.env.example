# Optional public site URL (used for absolute links in production if needed)
# NEXT_PUBLIC_SITE_URL=https://your-render-service.onrender.com

# Avion backend API (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:4000
