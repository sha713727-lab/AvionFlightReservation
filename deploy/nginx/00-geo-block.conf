# Geo access policy for aviosupportdesk.com
# default 0 = ALLOW (UK, Europe, USA, Canada, and the rest of the world)
# CIDRs marked 1 = BLOCK (see deploy/geo/blocked-countries.txt)
geo $remote_addr $geo_blocked {
    default 0;
    include /etc/nginx/geo/blocked-cidrs.conf;
}
